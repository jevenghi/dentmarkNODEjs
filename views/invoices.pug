extends base
block styles
  link(rel="stylesheet" href="/css/style.css")
block content
  .main-container
    .customers-invoice-menu
      .customers-invoice-dropdown
        select#customers-invoice
          option(value="" disabled selected data-key="selectCustomer")
          each customer in customers
            option(value=customer._id selected=(customer._id.toString() === selectedUser))= he.decode(customer.name)
      .invoice-buttons-top        
        button.show-invoice-address-btn(data-key="showInvoiceAddress")
        button.create-invoice-btn(data-key="previewInvoice")

    form(class='form-user-invoice-address' style='display: none;')
      button(type="button" class="close-invoice-address-btn" aria-label="Close") ×
      div.form-group
        label(for="name" data-key="companyName")
        input(type="text" id="name" name="name" value=`` required)
      div.form-group
        label(for="street" data-key="streetHouseNumber")
        input(type="text" id="street" name="street" value=`` required)
      div.form-group
        label(for="city" data-key="postcodeCity")
        input(type="text" id="city" name="city" value=`` required)   
      div.form-group
        label(for="city" data-key="emailAddress")
        input(type="text" id="email" name="email" value=`` required)
      
      button(type="submit" class="save-invoice-changes-btn" data-key="saveChanges")      
    if tasks
      table.task-table
          thead
          tr
              th(data-key="vehicleModel")
              th(data-key="taskStatus")
              th(data-key="taskCost")
              th(data-key="taskCreated")
              th(data-key="taskCompleted")
              //- th Invoice Sent
          tbody
          if tasks.length === 0
              tr
              td(colspan='6' data-key="noTasksFound")
          each task in tasks
              tr
              td: a(href=`/tasks/${task._id}` class='table-link')= task.carModel
              td(class='clickable-status' id=task._id data-status=task.taskStatus)= task.taskStatus
              td(class='clickable-status' id=task._id)= task.totalCost
              td(class='clickable-status' id=task._id)= new Date(task.createdAt).toLocaleDateString('en-US', { month: 'short', day: '2-digit', year: 'numeric' })
              td
                  if task.completedAt
                  = new Date(task.completedAt).toLocaleDateString('en-US', { month: 'short', day: '2-digit', year: 'numeric' })
    //- div.pagination-buttons
    //-   if page > 1
    //-     button.previous-button() Prev
    //-   //- if page < totalPageCount
    //-   //-   button.next-button() Next
    //-   if tasks.length === limit
    //-     button.next-button() Next
    div(class='invoice-form' style='display: none')
      div.spinner-overlay(style='display: none;')
        div.spinner
      div.top-right
        span.company-name
        span.street
        span.postcode-city
        span.email
        
      div.row-3-inputs
        span.invoice-date
        input(type="text" id='invoice-number')
        span.invoice-expiry-date
        
      div.bottom-right
        span.total-excl
        span.btw         
        span.total-incl
      div.save-invoice-buttons
        button.save-invoice-btn(data-key="downloadInvoice")
      //-   button.send-invoice-btn Send invoice
    

  .overlay.hidden            
        
